namespace BasicWebServer.Server.HTTP
{
    public enum StatusCode
    {
        OK = 200,
            Found = 301,
            NotFound = 404,
            Unauthorized = 401, 
            BadRequest = 400
    }
}
